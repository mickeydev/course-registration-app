export default interface AddInstructor {
  title: string;
  name: string;
  department: string;
}

export interface ViewInstructor {
  id: number;
  title: string;
  name: string;
  department: string;
}
