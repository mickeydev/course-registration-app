export default interface AddStudent {
  firstName: string;
  lastName: string;
  program: string;
}

export interface ViewStudent {
  id: number;
  firstName: string;
  lastName: string;
  program: string;
}
